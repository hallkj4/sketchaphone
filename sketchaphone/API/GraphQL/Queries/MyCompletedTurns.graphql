query myCompletedTurns($limit: Int, $nextToken: String) {
    myCompletedTurns(limit: $limit, nextToken: $nextToken) {
        turns {
            game {
                ...GameDetailed
            }
        }
        nextToken
    }
}

